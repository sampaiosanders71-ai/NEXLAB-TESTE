(function(){
  'use strict';
  const TEAM_ICON_SVG_SHAPES_V02682={"badge-check":[["path",{"d":"M3.85 8.62a4 4 0 0 1 4.78-4.77 4 4 0 0 1 6.74 0 4 4 0 0 1 4.78 4.78 4 4 0 0 1 0 6.74 4 4 0 0 1-4.77 4.78 4 4 0 0 1-6.75 0 4 4 0 0 1-4.78-4.77 4 4 0 0 1 0-6.76Z"}],["path",{"d":"m9 12 2 2 4-4"}]],"briefcase-business":[["path",{"d":"M12 12h.01"}],["path",{"d":"M16 6V4a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"}],["path",{"d":"M22 13a18.15 18.15 0 0 1-20 0"}],["rect",{"width":"20","height":"14","x":"2","y":"6","rx":"2"}]],"calendar-days":[["path",{"d":"M8 2v4"}],["path",{"d":"M16 2v4"}],["rect",{"width":"18","height":"18","x":"3","y":"4","rx":"2"}],["path",{"d":"M3 10h18"}],["path",{"d":"M8 14h.01"}],["path",{"d":"M12 14h.01"}],["path",{"d":"M16 14h.01"}],["path",{"d":"M8 18h.01"}],["path",{"d":"M12 18h.01"}],["path",{"d":"M16 18h.01"}]],"chart-no-axes-combined":[["path",{"d":"M12 16v5"}],["path",{"d":"M16 14v7"}],["path",{"d":"M20 10v11"}],["path",{"d":"m22 3-8.646 8.646a.5.5 0 0 1-.708 0L9.354 8.354a.5.5 0 0 0-.707 0L2 15"}],["path",{"d":"M4 18v3"}],["path",{"d":"M8 14v7"}]],"circle-dollar-sign":[["circle",{"cx":"12","cy":"12","r":"10"}],["path",{"d":"M16 8h-6a2 2 0 1 0 0 4h4a2 2 0 1 1 0 4H8"}],["path",{"d":"M12 18V6"}]],"clipboard-list":[["rect",{"width":"8","height":"4","x":"8","y":"2","rx":"1","ry":"1"}],["path",{"d":"M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"}],["path",{"d":"M12 11h4"}],["path",{"d":"M12 16h4"}],["path",{"d":"M8 11h.01"}],["path",{"d":"M8 16h.01"}]],"code-2":[["path",{"d":"m18 16 4-4-4-4"}],["path",{"d":"m6 8-4 4 4 4"}],["path",{"d":"m14.5 4-5 16"}]],"compass":[["path",{"d":"m16.24 7.76-1.804 5.411a2 2 0 0 1-1.265 1.265L7.76 16.24l1.804-5.411a2 2 0 0 1 1.265-1.265z"}],["circle",{"cx":"12","cy":"12","r":"10"}]],"files":[["path",{"d":"M20 7h-3a2 2 0 0 1-2-2V2"}],["path",{"d":"M9 18a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h7l4 4v10a2 2 0 0 1-2 2Z"}],["path",{"d":"M3 7.6v12.8A1.6 1.6 0 0 0 4.6 22h9.8"}]],"graduation-cap":[["path",{"d":"M21.42 10.922a1 1 0 0 0-.019-1.838L12.83 5.18a2 2 0 0 0-1.66 0L2.6 9.08a1 1 0 0 0 0 1.832l8.57 3.908a2 2 0 0 0 1.66 0z"}],["path",{"d":"M22 10v6"}],["path",{"d":"M6 12.5V16a6 3 0 0 0 12 0v-3.5"}]],"headset":[["path",{"d":"M3 11h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-5Zm0 0a9 9 0 1 1 18 0m0 0v5a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2h3Z"}],["path",{"d":"M21 16v2a4 4 0 0 1-4 4h-5"}]],"lightbulb":[["path",{"d":"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"}],["path",{"d":"M9 18h6"}],["path",{"d":"M10 22h4"}]],"megaphone":[["path",{"d":"m3 11 18-5v12L3 14v-3z"}],["path",{"d":"M11.6 16.8a3 3 0 1 1-5.8-1.6"}]],"messages-square":[["path",{"d":"M14 9a2 2 0 0 1-2 2H6l-4 4V4a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2z"}],["path",{"d":"M18 9h2a2 2 0 0 1 2 2v11l-4-4h-6a2 2 0 0 1-2-2v-1"}]],"microscope":[["path",{"d":"M6 18h8"}],["path",{"d":"M3 22h18"}],["path",{"d":"M14 22a7 7 0 1 0 0-14h-1"}],["path",{"d":"M9 14h2"}],["path",{"d":"M9 12a2 2 0 0 1-2-2V6h6v4a2 2 0 0 1-2 2Z"}],["path",{"d":"M12 6V3a1 1 0 0 0-1-1H9a1 1 0 0 0-1 1v3"}]],"monitor-cog":[["path",{"d":"M12 17v4"}],["path",{"d":"m14.305 7.53.923-.382"}],["path",{"d":"m15.228 4.852-.923-.383"}],["path",{"d":"m16.852 3.228-.383-.924"}],["path",{"d":"m16.852 8.772-.383.923"}],["path",{"d":"m19.148 3.228.383-.924"}],["path",{"d":"m19.53 9.696-.382-.924"}],["path",{"d":"m20.772 4.852.924-.383"}],["path",{"d":"m20.772 7.148.924.383"}],["path",{"d":"M22 13v2a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h7"}],["path",{"d":"M8 21h8"}],["circle",{"cx":"18","cy":"6","r":"3"}]],"palette":[["path",{"d":"M12 22a1 1 0 0 1 0-20 10 9 0 0 1 10 9 5 5 0 0 1-5 5h-2.25a1.75 1.75 0 0 0-1.4 2.8l.3.4a1.75 1.75 0 0 1-1.4 2.8z"}],["circle",{"cx":"13.5","cy":"6.5","r":".5","fill":"currentColor"}],["circle",{"cx":"17.5","cy":"10.5","r":".5","fill":"currentColor"}],["circle",{"cx":"6.5","cy":"12.5","r":".5","fill":"currentColor"}],["circle",{"cx":"8.5","cy":"7.5","r":".5","fill":"currentColor"}]],"settings":[["path",{"d":"M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z"}],["circle",{"cx":"12","cy":"12","r":"3"}]],"target":[["circle",{"cx":"12","cy":"12","r":"10"}],["circle",{"cx":"12","cy":"12","r":"6"}],["circle",{"cx":"12","cy":"12","r":"2"}]],"users":[["path",{"d":"M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"}],["path",{"d":"M16 3.128a4 4 0 0 1 0 7.744"}],["path",{"d":"M22 21v-2a4 4 0 0 0-3-3.87"}],["circle",{"cx":"9","cy":"7","r":"4"}]]};

  const BUILD=globalThis.__NEXLAB_BUILD_IDENTITY__||Object.freeze({version:'0.26.82',revision:'beta-0-26-82-marketing-mensagem-removida'});
  const REVISION=BUILD.revision;
  if(globalThis.__NEXLAB_TEAM_CHAT__?.revision===REVISION)return;

  const UUID_RE=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
  const PANEL_SELECTOR='.team-details-v2680[data-nexlab-team-id]';
  const CONTENT_SELECTOR='.team-details-v2680__content';
  const HEADER_SELECTOR='.team-details-v2680__header';
  const WORKER_FALLBACK='https://nexlab-communication.sampaiosanders71.workers.dev';
  const PAGE_SIZE=30;
  const states=new WeakMap();
  const stateByTeamId=new Map();
  let profilesPromise=null;
  let observerScheduled=false;
  let pendingNotification=null;
  let notificationPromise=null;

  function client(){return globalThis.__NEXLAB_SUPABASE__||null;}
  function workerUrl(){return String(globalThis.__NEXLAB_CONFIG__?.endpoints?.communication||WORKER_FALLBACK).replace(/\/+$/,'');}
  function isUuid(value){return UUID_RE.test(String(value||''));}
  function clean(value,max=4000){return String(value??'').trim().slice(0,max);}
  function initials(name){return clean(name,80).split(/\s+/).filter(Boolean).slice(0,2).map(v=>v[0]?.toUpperCase()||'').join('')||'U';}
  function roleLabel(role){return ({admin:'Admin',administrador:'Admin',coordenador:'Coordenador',bolsista:'Bolsista',voluntario:'Voluntário',coworking_junior:'Coworking Júnior'}[String(role||'').toLowerCase()]||'Usuário');}
  function formatDate(value){if(!value)return 'Agora';try{return new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'}).format(new Date(value));}catch{return '—';}}
  function formatTime(value){if(!value)return 'Agora';try{return new Intl.DateTimeFormat('pt-BR',{hour:'2-digit',minute:'2-digit'}).format(new Date(value));}catch{return '—';}}
  function chatDayKey(value){try{const d=new Date(value);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;}catch{return 'unknown';}}
  function chatDayLabel(value){try{const d=new Date(value),now=new Date(),today=new Date(now.getFullYear(),now.getMonth(),now.getDate()),target=new Date(d.getFullYear(),d.getMonth(),d.getDate()),days=Math.round((today-target)/86400000);if(days===0)return 'Hoje';if(days===1)return 'Ontem';return new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'short',year:d.getFullYear()!==now.getFullYear()?'numeric':undefined}).format(d).replace('.','');}catch{return 'Mensagens';}}

  function node(tag,className,text){const el=document.createElement(tag);if(className)el.className=className;if(text!=null)el.textContent=String(text);return el;}
  const TEAM_CHAT_UI_ICONS_V02682=Object.freeze({
    overview:[['rect',{x:'3',y:'3',width:'7',height:'7',rx:'1'}],['rect',{x:'14',y:'3',width:'7',height:'7',rx:'1'}],['rect',{x:'3',y:'14',width:'7',height:'7',rx:'1'}],['rect',{x:'14',y:'14',width:'7',height:'7',rx:'1'}]],
    tasks:[['path',{d:'M9 11l3 3L22 4'}],['path',{d:'M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11'}]],
    links:[['path',{d:'M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71'}],['path',{d:'M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71'}]],
    conversation:[['path',{d:'M21 15a4 4 0 0 1-4 4H8l-5 3V7a4 4 0 0 1 4-4h10a4 4 0 0 1 4 4z'}]],
    users:[['path',{d:'M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2'}],['circle',{cx:'9',cy:'7',r:'4'}],['path',{d:'M22 21v-2a4 4 0 0 0-3-3.87'}],['path',{d:'M16 3.13a4 4 0 0 1 0 7.75'}]],
    refresh:[['path',{d:'M20 11a8.1 8.1 0 0 0-15.5-2M4 4v5h5'}],['path',{d:'M4 13a8.1 8.1 0 0 0 15.5 2M20 20v-5h-5'}]],
    at:[['circle',{cx:'12',cy:'12',r:'4'}],['path',{d:'M16 8v5a3 3 0 0 0 6 0v-1a10 10 0 1 0-4 8'}]],
    send:[['path',{d:'m22 2-7 20-4-9-9-4Z'}],['path',{d:'M22 2 11 13'}]],
    reply:[['path',{d:'m9 17-5-5 5-5'}],['path',{d:'M4 12h10a6 6 0 0 1 6 6v1'}]],
    clipboard:[['rect',{x:'9',y:'2',width:'6',height:'4',rx:'1'}],['path',{d:'M9 4H6a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V6a2 2 0 0 0-2-2h-3'}]],
    more:[['circle',{cx:'5',cy:'12',r:'1'}],['circle',{cx:'12',cy:'12',r:'1'}],['circle',{cx:'19',cy:'12',r:'1'}]],
    chevron:[['path',{d:'m9 18 6-6-6-6'}]],
    search:[['circle',{cx:'11',cy:'11',r:'7'}],['path',{d:'m21 21-4.35-4.35'}]],
    filter:[['polygon',{points:'22 3 2 3 10 12.46 10 19 14 21 14 12.46 22 3'}]],
    plus:[['path',{d:'M12 5v14'}],['path',{d:'M5 12h14'}]],
    clock:[['circle',{cx:'12',cy:'12',r:'9'}],['path',{d:'M12 7v5l3 2'}]],
    play:[['path',{d:'M8 5v14l11-7z'}]],
    'check-circle':[['circle',{cx:'12',cy:'12',r:'9'}],['path',{d:'m9 12 2 2 4-4'}]],
    calendar:[['rect',{x:'3',y:'4',width:'18',height:'18',rx:'2'}],['path',{d:'M16 2v4'}],['path',{d:'M8 2v4'}],['path',{d:'M3 10h18'}]],
    dot:[['circle',{cx:'12',cy:'12',r:'2.25'}]]
  });
  function teamChatUiIcon(key,size=16){const svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.setAttribute('viewBox','0 0 24 24');svg.setAttribute('fill','none');svg.setAttribute('stroke','currentColor');svg.setAttribute('stroke-width','2');svg.setAttribute('stroke-linecap','round');svg.setAttribute('stroke-linejoin','round');svg.setAttribute('width',String(size));svg.setAttribute('height',String(size));svg.setAttribute('aria-hidden','true');for(const [tag,attrs] of TEAM_CHAT_UI_ICONS_V02682[key]||[]){const child=document.createElementNS('http://www.w3.org/2000/svg',tag);for(const [name,value] of Object.entries(attrs))child.setAttribute(name,String(value));svg.appendChild(child);}return svg;}
  const TEAM_COLOR_VALUES_V02682=Object.freeze({blue:'#2563eb',orange:'#f97316',green:'#16a34a',purple:'#7c3aed',cyan:'#0891b2',pink:'#db2777',amber:'#d97706'});
  const TEAM_ICON_KEYS_V02682=new Set(['users','code-2','monitor-cog','megaphone','messages-square','palette','clipboard-list','briefcase-business','circle-dollar-sign','microscope','lightbulb','graduation-cap','calendar-days','target','chart-no-axes-combined','headset','files','badge-check','compass','settings']);
  function teamIdentity(state){const panel=state?.panel;const iconKey=TEAM_ICON_KEYS_V02682.has(String(panel?.dataset?.nexlabTeamIcon||''))?String(panel.dataset.nexlabTeamIcon):'users';const colorKey=TEAM_COLOR_VALUES_V02682[String(panel?.dataset?.nexlabTeamColor||'')]?String(panel.dataset.nexlabTeamColor):'blue';return {iconKey,colorKey,color:TEAM_COLOR_VALUES_V02682[colorKey],name:clean(panel?.dataset?.nexlabTeamName||'Equipe',120)||'Equipe'};}
  function teamIdentityIcon(state,size=26){
    const identity=teamIdentity(state);
    const svg=document.createElementNS('http://www.w3.org/2000/svg','svg');
    svg.setAttribute('class','nexlab-team-chat-identity-icon-v02682');
    svg.setAttribute('aria-hidden','true');
    svg.setAttribute('focusable','false');
    svg.setAttribute('viewBox','0 0 24 24');
    svg.setAttribute('fill','none');
    svg.setAttribute('stroke','currentColor');
    svg.setAttribute('stroke-width','2');
    svg.setAttribute('stroke-linecap','round');
    svg.setAttribute('stroke-linejoin','round');
    svg.setAttribute('width',String(size));
    svg.setAttribute('height',String(size));
    svg.style.width=`${size}px`;
    svg.style.height=`${size}px`;
    svg.style.color=identity.color;
    svg.style.setProperty('--nexlab-team-chat-accent',identity.color);
    const shapes=TEAM_ICON_SVG_SHAPES_V02682[identity.iconKey]||TEAM_ICON_SVG_SHAPES_V02682.users;
    shapes.forEach(([tag,attrs])=>{const child=document.createElementNS('http://www.w3.org/2000/svg',tag);Object.entries(attrs).forEach(([key,value])=>{const attr=key==='strokeWidth'?'stroke-width':key==='strokeLinecap'?'stroke-linecap':key==='strokeLinejoin'?'stroke-linejoin':key==='fillRule'?'fill-rule':key==='clipRule'?'clip-rule':key;child.setAttribute(attr,String(value));});svg.appendChild(child);});
    return svg;
  }
  function profile(state,id){return state.profiles?.get?.(String(id))||null;}
  function profileName(state,id){return profile(state,id)?.nome||'Usuário';}

  function toast(message,type='info'){
    try{globalThis.dispatchEvent(new CustomEvent('nexlab:toast',{detail:{text:message,type}}));}catch{}
  }

  async function waitClient(timeout=12000){
    const started=Date.now();
    while(Date.now()-started<timeout){const c=client();if(c?.auth)return c;await new Promise(resolve=>setTimeout(resolve,80));}
    throw new Error('Cliente do NEXLAB não ficou disponível.');
  }

  async function session(){
    const c=await waitClient();
    const result=await c.auth.getSession();
    if(result?.error)throw result.error;
    const current=result?.data?.session||null;
    if(!current?.access_token)throw new Error('Sessão expirada. Entre novamente no NEXLAB.');
    return current;
  }

  async function api(path,options={},retry=true){
    const current=await session();
    const headers=new Headers(options.headers||{});
    headers.set('Authorization',`Bearer ${current.access_token}`);
    headers.set('Accept','application/json');
    if(options.body!=null&&!headers.has('Content-Type'))headers.set('Content-Type','application/json');
    let response;
    try{
      const controller=new AbortController();
      const timer=setTimeout(()=>controller.abort(),15000);
      try{response=await fetch(workerUrl()+path,{...options,headers,signal:controller.signal,cache:'no-store'});}finally{clearTimeout(timer);}
    }catch(error){
      if(error?.name==='AbortError')throw new Error('O serviço de conversa demorou para responder.');
      throw new Error(navigator.onLine===false?'Sem conexão com a internet.':'Não foi possível acessar o serviço de conversa.');
    }
    if(response.status===401&&retry){try{await client()?.auth?.refreshSession?.();}catch{}return api(path,options,false);}
    let data=null;try{data=await response.json();}catch{}
    if(!response.ok){
      const code=String(data?.error||`http_${response.status}`);
      const messages={
        access_denied:'Você não possui acesso à conversa desta equipe.',
        invalid_or_expired_session:'Sua sessão expirou. Entre novamente.',
        team_not_found:'Esta equipe não está mais disponível.',
        invalid_mention_target:'Uma das pessoas mencionadas não possui acesso a esta conversa.',
        mention_validation_denied:'Não foi possível validar as menções.',
        text_too_long:'A mensagem excede o limite permitido.',
        text_required:'Digite uma mensagem antes de enviar.'
      };
      const error=new Error(messages[code]||'Não foi possível concluir a ação.');error.code=code;error.status=response.status;throw error;
    }
    return data;
  }

  async function loadProfiles(){
    if(profilesPromise)return profilesPromise;
    profilesPromise=(async()=>{
      const c=await waitClient();
      const {data,error}=await c.rpc('nexlab_list_profiles_visible_v26311');
      if(error)throw error;
      const map=new Map();for(const row of Array.isArray(data)?data:[]){if(row?.id)map.set(String(row.id),row);}return map;
    })().catch(error=>{profilesPromise=null;console.warn('NEXLAB team chat profiles',error);return new Map();});
    return profilesPromise;
  }

  async function loadAccess(state){
    if(state.accessLoading)return state.accessLoading;
    state.accessLoading=(async()=>{
      const c=await waitClient();
      const [{data,error},profiles,current]=await Promise.all([
        c.rpc('nexlab_get_communication_access_v1',{p_scope:'team',p_scope_id:state.teamId}),
        loadProfiles(),session()
      ]);
      if(error)throw error;
      state.access=Array.isArray(data)?data[0]:data;
      state.profiles=profiles;
      state.currentUserId=String(current.user?.id||'');
      syncNavigation(state);renderComposer(state);
      return state.access;
    })().catch(error=>{state.accessError=error;console.error('NEXLAB team chat access',error);syncNavigation(state);renderComposer(state);return null;}).finally(()=>{state.accessLoading=null;});
    return state.accessLoading;
  }

  function buildTabs(state){
    const nav=node('nav','nexlab-team-tabs-rebuilt-v02682');nav.setAttribute('aria-label','Áreas da equipe');nav.setAttribute('role','tablist');
    const tabs=[['overview','Visão geral','overview'],['tasks','Tarefas','tasks'],['links','Vínculos','links'],['conversation','Conversa','conversation']];
    for(const [key,label,icon] of tabs){
      const button=node('button','nexlab-team-tab-rebuilt-v02682');button.type='button';button.dataset.teamTab=key;button.setAttribute('role','tab');button.setAttribute('aria-selected','false');button.append(teamChatUiIcon(icon,15),node('span','',label));button.addEventListener('click',()=>activateTab(state,key));nav.appendChild(button);
    }
    return nav;
  }

  function buildConversationPanel(state){
    const section=node('section','nexlab-team-chat-rebuilt-v02682');section.hidden=true;section.setAttribute('aria-label','Conversa da equipe');
    const head=node('header','nexlab-team-chat-header-rebuilt-v02682');
    const identity=teamIdentity(state);
    const title=node('div','nexlab-team-chat-title-rebuilt-v02682');
    const iconShell=node('span','nexlab-team-chat-identity-shell-rebuilt-v02682');iconShell.style.setProperty('--nexlab-team-chat-accent',identity.color);iconShell.appendChild(teamIdentityIcon(state,20));
    const copy=node('div','nexlab-team-chat-title-copy-rebuilt-v02682');copy.append(node('h3','', 'Conversa da equipe'),node('p','', identity.name));title.append(iconShell,copy);
    const actions=node('div','nexlab-team-chat-header-actions-rebuilt-v02682');
    const members=node('button','nexlab-team-chat-members-rebuilt-v02682');members.type='button';members.append(teamChatUiIcon('users',15),node('span','', 'Ver membros'),teamChatUiIcon('chevron',12));members.addEventListener('click',()=>{const footer=state.panel.querySelector(':scope > .team-details-v2680__footer');const button=[...(footer?.querySelectorAll('button')||[])].find(el=>/integrantes/i.test(clean(el.textContent,80)));if(button)button.click();});
    const refresh=node('button','nexlab-team-chat-refresh-rebuilt-v02682');refresh.type='button';refresh.setAttribute('aria-label','Atualizar conversa');refresh.title='Atualizar conversa';refresh.appendChild(teamChatUiIcon('refresh',15));refresh.addEventListener('click',()=>loadMessages(state,{force:true}));
    actions.append(members,refresh);head.append(title,actions);
    const context=node('div','nexlab-team-chat-context-rebuilt-v02682 is-checking');context.dataset.connectionState='checking';const contextDot=node('span','nexlab-team-chat-context-dot-rebuilt-v02682');const contextCopy=node('div','nexlab-team-chat-context-copy-rebuilt-v02682');contextCopy.append(node('strong','', 'Verificando conexão'),node('span','', 'Aguardando resposta do serviço de mensagens.'));context.append(contextDot,contextCopy);
    const notice=node('div','nexlab-team-chat-notice-rebuilt-v02682');notice.hidden=true;notice.setAttribute('role','status');
    const older=node('button','nexlab-team-chat-older-rebuilt-v02682','Carregar mensagens anteriores');older.type='button';older.hidden=true;older.addEventListener('click',()=>loadOlder(state));
    const list=node('div','nexlab-team-chat-list-rebuilt-v02682');list.setAttribute('aria-live','polite');
    const composer=node('div','nexlab-team-chat-composer-slot-rebuilt-v02682');
    section.append(head,context,notice,older,list,composer);return section;
  }

  function buildTasksPanel(state){
    const section=node('section','nexlab-team-tasks-rebuilt-v02682');section.hidden=true;section.setAttribute('aria-label','Tarefas da equipe');
    const shell=node('div','nexlab-team-tasks-shell-v02682');
    const heading=node('header','nexlab-team-tasks-heading-v02682');
    const headingCopy=node('div','nexlab-team-tasks-heading-copy-v02682');headingCopy.append(node('h3','', 'Tarefas da equipe'),node('p','', 'Acompanhe e gerencie as tarefas da equipe.'));
    heading.appendChild(headingCopy);

    const toolbar=node('div','nexlab-team-tasks-toolbar-v02682');
    const search=node('label','nexlab-team-task-search-v02682');
    search.appendChild(teamChatUiIcon('search',16));
    const searchInput=document.createElement('input');searchInput.type='search';searchInput.placeholder='Buscar tarefa...';searchInput.setAttribute('aria-label','Buscar tarefa');searchInput.autocomplete='off';searchInput.value=state.taskSearchTerm||'';searchInput.addEventListener('input',()=>{state.taskSearchTerm=searchInput.value||'';renderTeamTasks(state);});search.appendChild(searchInput);

    const toolbarActions=node('div','nexlab-team-task-toolbar-actions-v02682');
    const filtersWrap=node('div','nexlab-team-task-filters-wrap-v02682');
    const filters=node('button','nexlab-team-task-filter-toggle-v02682');filters.type='button';filters.setAttribute('aria-haspopup','menu');filters.setAttribute('aria-expanded',String(!!state.taskFilterPanelOpen));filters.append(teamChatUiIcon('filter',14),node('span','', 'Filtros'));filters.addEventListener('click',()=>{state.taskFilterPanelOpen=!state.taskFilterPanelOpen;renderTeamTasks(state);});
    const filtersPanel=node('div','nexlab-team-task-filter-panel-v02682');filtersPanel.hidden=true;filtersPanel.setAttribute('role','menu');
    const filterOptions=[['all','Todas'],['mine','Minhas'],['overdue','Atrasadas'],['scheduled','Com prazo']];
    for(const [value,label] of filterOptions){const option=node('button','nexlab-team-task-filter-option-v02682',label);option.type='button';option.setAttribute('role','menuitemradio');option.dataset.quickFilter=value;option.addEventListener('click',()=>{state.taskQuickFilter=value;state.taskFilterPanelOpen=false;renderTeamTasks(state);});filtersPanel.appendChild(option);}    
    filtersWrap.append(filters,filtersPanel);

    const create=node('button','nexlab-team-task-create-v02682');create.type='button';create.hidden=true;create.append(teamChatUiIcon('plus',15),node('span','', 'Nova tarefa'));create.addEventListener('click',()=>showTaskForm(state));

    toolbarActions.append(filtersWrap,create);toolbar.append(search,toolbarActions);

    const summary=node('div','nexlab-team-task-summary-v02682');
    const notice=node('div','nexlab-team-task-message-v02682');notice.hidden=true;notice.setAttribute('role','status');
    const form=node('div','nexlab-team-task-form-slot-v02682');form.hidden=true;
    const detail=node('div','nexlab-team-task-detail-slot-v02682');detail.hidden=true;
    const list=node('div','nexlab-team-task-groups-v02682');list.setAttribute('aria-live','polite');

    shell.append(heading,toolbar,summary,notice,form,detail,list);section.appendChild(shell);return section;
  }

  function buildTeamWorkspace(tasks,chat){
    const workspace=node('div','nexlab-team-workspace-v057');workspace.hidden=true;workspace.setAttribute('aria-label','Área de trabalho da equipe');workspace.append(tasks,chat);return workspace;
  }

  function setTaskMessage(state,message,type='info'){
    const box=state.tasksPanel?.querySelector('.nexlab-team-task-message-v02682');if(!box)return;
    box.hidden=!message;box.className=`nexlab-team-task-message-v02682 is-${type}`;box.textContent=message||'';
  }

  function formatTaskDay(value){if(!value)return 'Sem prazo';try{return new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'2-digit',year:'numeric'}).format(new Date(`${String(value).slice(0,10)}T12:00:00`));}catch{return String(value);}}
  function formatTaskMoment(value){if(!value)return '';try{return new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'2-digit',year:'numeric'}).format(new Date(value));}catch{return '';}}
  function taskPriorityLabel(value){return ({baixa:'Baixa',normal:'Normal',alta:'Alta',urgente:'Urgente'}[String(value||'').toLowerCase()]||'Normal');}
  function normalizeTaskStatus(value){const key=String(value||'').toLowerCase();if(key==='em andamento')return 'em_andamento';if(key==='concluída')return 'concluida';return ({pendente:'pendente',em_andamento:'em_andamento',concluida:'concluida',cancelada:'cancelada'}[key]||'pendente');}
  function taskStatusLabel(value){return ({pendente:'Pendentes',em_andamento:'Em andamento',concluida:'Concluídas',cancelada:'Canceladas'}[normalizeTaskStatus(value)]||'Pendentes');}
  function taskSummaryLabel(key){return ({total:'todas as tarefas',pendente:'aguardando início',em_andamento:'em progresso',concluida:'finalizadas'}[key]||'todas as tarefas');}
  function isTaskOverdue(task){if(!task?.deadline||['concluida','cancelada'].includes(normalizeTaskStatus(task.status)))return false;const today=new Date();today.setHours(0,0,0,0);const due=new Date(`${String(task.deadline).slice(0,10)}T00:00:00`);return Number.isFinite(due.getTime())&&due<today;}
  function taskMetaLine(task){const status=normalizeTaskStatus(task?.status);const creator=clean(task?.created_by_name||task?.creator_name||task?.author_name||'Equipe',80)||'Equipe';if(status==='concluida'){const who=clean(task?.completed_by_name||task?.responsible_name||creator,80)||creator;const date=formatTaskMoment(task?.completed_at||task?.updated_at)||formatTaskDay(task?.deadline);return date?`Concluída por ${who} • Concluída em: ${date}`:`Concluída por ${who}`;}if(status==='em_andamento'){const started=formatTaskMoment(task?.started_at||task?.updated_at);return started?`Criada por ${creator} • Iniciada em: ${started}`:`Criada por ${creator}`;}if(status==='cancelada'){const canceled=formatTaskMoment(task?.updated_at);return canceled?`Criada por ${creator} • Cancelada em: ${canceled}`:`Criada por ${creator}`;}const due=task?.deadline?formatTaskDay(task.deadline):'';return due?`Criada por ${creator} • Prazo: ${due}`:`Criada por ${creator}`;}
  function taskDateBadge(task){const status=normalizeTaskStatus(task?.status);if(status==='concluida'){return {icon:'check-circle',label:formatTaskMoment(task?.completed_at)||formatTaskDay(task?.deadline)};}return {icon:'calendar',label:formatTaskDay(task?.deadline)};}
  function taskGroupTone(key){return ({pendente:'pending',em_andamento:'progress',concluida:'done',cancelada:'muted'}[normalizeTaskStatus(key)]||'pending');}
  function taskMatchesSearch(task,query){const q=clean(query||'',120).toLowerCase();if(!q)return true;const hay=[task?.title,task?.description,task?.responsible_name,task?.created_by_name,task?.creator_name,task?.author_name,taskPriorityLabel(task?.priority),taskStatusLabel(task?.status)].map(value=>String(value||'').toLowerCase()).join(' ');return hay.includes(q);}
  function taskMatchesQuickFilter(state,task){const mode=String(state.taskQuickFilter||'all');if(mode==='mine')return String(task?.responsible_id||'')===String(state.currentUserId||'');if(mode==='overdue')return isTaskOverdue(task);if(mode==='scheduled')return !!task?.deadline;return true;}
  function taskGroupCatalog(){return [{key:'pendente',label:'Pendentes'},{key:'em_andamento',label:'Em andamento'},{key:'concluida',label:'Concluídas'},{key:'cancelada',label:'Canceladas'}];}

  function taskWorkspaceReadOnly(workspace){
    if(!workspace)return false;
    return !!(workspace.read_only||workspace.team_archived||workspace.archived||workspace.team?.archived_at||workspace.team?.archived===true);
  }
  function taskStatusActionLabel(status){
    return ({pendente:'Iniciar tarefa',em_andamento:'Voltar para pendente',concluida:'Reabrir tarefa',cancelada:'Reabrir tarefa'}[normalizeTaskStatus(status)]||'Alterar status');
  }
  function taskNextStatus(status){
    return ({pendente:'em_andamento',em_andamento:'pendente',concluida:'em_andamento',cancelada:'pendente'}[normalizeTaskStatus(status)]||'pendente');
  }
  function taskDetailValue(label,value){const row=node('div','nexlab-team-task-detail-row-v02682');row.append(node('span','',label),node('strong','',value||'—'));return row;}
  function closeTaskDetail(state){const slot=state.tasksPanel?.querySelector('.nexlab-team-task-detail-slot-v02682');if(slot){slot.hidden=true;slot.replaceChildren();}state.taskDetailId='';}
  function focusTaskMenu(state,taskId){
    if(!taskId||!state.tasksPanel)return;
    const row=state.tasksPanel.querySelector(`[data-task-id="${CSS.escape(String(taskId))}"]`);
    row?.querySelector('.nexlab-team-task-row-menu-v02682 [role="menuitem"]')?.focus();
  }
  function focusTaskMenuToggle(state,taskId){
    if(!taskId||!state.tasksPanel)return;
    const row=state.tasksPanel.querySelector(`[data-task-id="${CSS.escape(String(taskId))}"]`);
    row?.querySelector('.nexlab-team-task-row-menu-toggle-v02682')?.focus();
  }
  function showTaskDetails(state,task){
    const slot=state.tasksPanel?.querySelector('.nexlab-team-task-detail-slot-v02682');if(!slot||!task)return;
    state.taskOpenMenuId='';state.taskDetailId=String(task.id||'');slot.hidden=false;slot.replaceChildren();
    const card=node('section','nexlab-team-task-detail-card-v02682');card.dataset.taskDetailId=state.taskDetailId;
    const head=node('div','nexlab-team-task-detail-head-v02682');
    const title=node('div');title.append(node('small','',taskStatusLabel(task.status)),node('h4','',task.title||'Tarefa'));
    const close=node('button','nexlab-team-task-detail-close-v02682');close.type='button';close.setAttribute('aria-label','Fechar detalhes');close.textContent='×';close.addEventListener('click',()=>closeTaskDetail(state));head.append(title,close);card.appendChild(head);
    if(task.description)card.appendChild(node('p','nexlab-team-task-detail-description-v02682',task.description));
    const grid=node('div','nexlab-team-task-detail-grid-v02682');grid.append(
      taskDetailValue('Responsável',task.responsible_name||'Não atribuído'),
      taskDetailValue('Prazo',formatTaskDay(task.deadline)),
      taskDetailValue('Prioridade',taskPriorityLabel(task.priority)),
      taskDetailValue('Situação',taskStatusLabel(task.status).replace(/s$/,''))
    );card.appendChild(grid);
    const meta=node('p','nexlab-team-task-detail-meta-v02682',taskMetaLine(task));card.appendChild(meta);
    const actions=node('div','nexlab-team-task-detail-actions-v02682');
    const readonly=taskWorkspaceReadOnly(state.taskWorkspace);
    if(task.can_edit&&!readonly){const edit=node('button','','Editar tarefa');edit.type='button';edit.addEventListener('click',()=>{closeTaskDetail(state);showTaskForm(state,task);});actions.appendChild(edit);}
    if(task.can_complete&&normalizeTaskStatus(task.status)!=='concluida'&&!readonly){const done=node('button','is-primary','Concluir');done.type='button';done.addEventListener('click',()=>{closeTaskDetail(state);completeTeamTask(state,task);});actions.appendChild(done);}
    if(task.can_delete&&!readonly){const remove=node('button','is-danger','Excluir');remove.type='button';remove.addEventListener('click',()=>{closeTaskDetail(state);deleteTeamTask(state,task);});actions.appendChild(remove);}
    if(actions.childElementCount)card.appendChild(actions);slot.appendChild(card);
    requestAnimationFrame(()=>card.scrollIntoView({behavior:'smooth',block:'nearest'}));
  }
  async function updateTeamTaskStatus(state,task,status){
    if(!task?.can_edit||taskWorkspaceReadOnly(state.taskWorkspace))return;
    try{
      setTaskMessage(state,'Atualizando situação da tarefa...','info');
      const c=await waitClient();
      const {data,error}=await c.rpc('nexlab_manage_team_task_v1',{p_team_id:state.teamId,p_action:'update',p_task_id:task.id,p_title:task.title||'Tarefa',p_description:task.description||null,p_responsible_id:task.responsible_id||null,p_deadline:task.deadline||null,p_priority:task.priority||'normal',p_status:status});
      if(error)throw error;if(!data?.ok)throw new Error('O Supabase não confirmou a mudança de situação.');
      state.taskWorkspace=data.workspace||state.taskWorkspace;state.tasksLoaded=true;state.taskOpenMenuId='';closeTaskDetail(state);renderTeamTasks(state);setTaskMessage(state,'Situação da tarefa atualizada.','ok');
      try{globalThis.__NEXLAB_PENDING_REALTIME_HUB__?.refresh?.({table:'tasks',eventType:'LOCAL_MUTATION'});globalThis.dispatchEvent(new CustomEvent('nexlab:pending-refresh'));}catch{}
    }catch(error){console.error('NEXLAB team task status',error);setTaskMessage(state,error?.message||'Não foi possível alterar a situação da tarefa.','err');}
  }

  function bindTaskRealtime(state){
    if(state.taskRealtimeUnsubscribe)return;
    const subscribe=globalThis.__NEXLAB_PENDING_REALTIME_HUB__?.subscribe;
    if(typeof subscribe!=='function')return;
    state.taskRealtimeUnsubscribe=subscribe(detail=>{
      if(detail?.table!=='team_history'||!state.tasksLoaded)return;
      if(detail.teamId&&String(detail.teamId)!==String(state.teamId))return;
      if(state.taskRealtimeTimer)clearTimeout(state.taskRealtimeTimer);
      state.taskRealtimeTimer=setTimeout(()=>{state.taskRealtimeTimer=null;void loadTeamTasks(state,{force:true});},250);
    });
  }

  async function loadTeamTasks(state,{force=false}={}){
    if(state.tasksLoading)return state.tasksLoading;
    const list=state.tasksPanel?.querySelector('.nexlab-team-task-groups-v02682');const refresh=state.tasksPanel?.querySelector('.nexlab-team-task-refresh-v02682');
    if(refresh){refresh.disabled=true;refresh.textContent='Atualizando...';}
    if(list&&(!state.tasksLoaded||force))list.classList.add('is-loading');
    setTaskMessage(state,'Carregando tarefas...','info');
    state.tasksLoading=(async()=>{
      const c=await waitClient();const {data,error}=await c.rpc('nexlab_get_team_tasks_v1',{p_team_id:state.teamId});if(error)throw error;
      if(!data?.ok)throw new Error('O Supabase não confirmou o carregamento das tarefas.');
      state.taskWorkspace=data;state.tasksLoaded=true;state.taskLoadError=null;setTaskMessage(state,'','info');renderTeamTasks(state);syncNavigation(state);return data;
    })().catch(error=>{console.error('NEXLAB team tasks load',error);state.taskLoadError=error;setTaskMessage(state,error?.message||'Não foi possível carregar as tarefas.','err');renderTeamTasks(state);return null;}).finally(()=>{
      state.tasksLoading=null;if(list)list.classList.remove('is-loading');if(refresh){refresh.disabled=false;refresh.textContent='Atualizar';}
    });
    return state.tasksLoading;
  }

  async function ensureTeamTasksReady(state){
    await loadAccess(state);
    if(!state.tasksLoaded&&!state.tasksLoading)await loadTeamTasks(state);
    if(state.targetTaskId)highlightTaskTarget(state,state.targetTaskId);
  }

  function taskField(label,input){const wrap=node('label','nexlab-team-task-field-v02682');wrap.append(node('span','',label),input);return wrap;}

  function showTaskForm(state,task=null,prefill=null){
    const permissions=state.taskWorkspace?.permissions||{};
    if(taskWorkspaceReadOnly(state.taskWorkspace)){setTaskMessage(state,'Esta equipe está em modo somente leitura.','err');return;}
    if(task&&!task.can_edit)return;
    if(!task&&!permissions.can_create){setTaskMessage(state,'Você não possui permissão para criar tarefas nesta equipe.','err');return;}
    const slot=state.tasksPanel?.querySelector('.nexlab-team-task-form-slot-v02682');if(!slot)return;
    const editing=!!task;slot.hidden=false;slot.replaceChildren();
    const form=node('form','nexlab-team-task-form-v02682');
    const formHead=node('div','nexlab-team-task-form-head-v02682');formHead.append(node('strong','',editing?'Editar tarefa':'Nova tarefa'),node('span','',editing?'Somente Líder ou Vice-Líder pode alterar os detalhes.':'Escolha um integrante da equipe como responsável.'));form.appendChild(formHead);
    const title=document.createElement('input');title.type='text';title.maxLength=180;title.required=true;title.value=task?.title||clean(prefill?.title||'',180);title.placeholder='Ex.: Preparar material da apresentação';
    const responsible=document.createElement('select');responsible.required=true;responsible.appendChild(new Option('Selecione o responsável',''));
    for(const member of state.taskWorkspace?.members||[]){const option=new Option(member.name||'Integrante',member.id);if(String(member.id)===String(task?.responsible_id||''))option.selected=true;responsible.appendChild(option);}
    const deadline=document.createElement('input');deadline.type='date';deadline.required=true;deadline.value=task?.deadline||'';
    const priority=document.createElement('select');for(const [value,label] of [['baixa','Baixa'],['normal','Normal'],['alta','Alta'],['urgente','Urgente']]){const option=new Option(label,value);if(value===(task?.priority||'normal'))option.selected=true;priority.appendChild(option);}
    const description=document.createElement('textarea');description.rows=3;description.maxLength=4000;description.value=task?.description||clean(prefill?.description||'',4000);description.placeholder='Descrição opcional';
    const grid=node('div','nexlab-team-task-form-grid-v02682');grid.append(taskField('Título',title),taskField('Responsável',responsible),taskField('Prazo',deadline),taskField('Prioridade',priority));form.append(grid,taskField('Descrição',description));
    let status=null;if(editing){status=document.createElement('select');const statusOptions=[['pendente','Pendente'],['em_andamento','Em andamento'],['cancelada','Cancelada']];if(task.can_complete||task.status==='concluida')statusOptions.splice(2,0,['concluida','Concluída']);for(const [value,label] of statusOptions){const option=new Option(label,value);if(value===(task.status||'pendente'))option.selected=true;status.appendChild(option);}form.appendChild(taskField('Status',status));}
    const actions=node('div','nexlab-team-task-form-actions-v02682');const cancel=node('button','','Cancelar');cancel.type='button';cancel.addEventListener('click',()=>{slot.hidden=true;slot.replaceChildren();});const save=node('button','is-primary',editing?'Salvar alterações':'Criar tarefa');save.type='submit';actions.append(cancel,save);form.appendChild(actions);
    form.addEventListener('submit',async event=>{
      event.preventDefault();save.disabled=true;cancel.disabled=true;save.textContent=editing?'Salvando...':'Criando...';
      try{
        const c=await waitClient();const args={p_team_id:state.teamId,p_action:editing?'update':'create',p_task_id:editing?task.id:null,p_title:title.value.trim(),p_description:description.value.trim()||null,p_responsible_id:responsible.value||null,p_deadline:deadline.value||null,p_priority:priority.value,p_status:editing?status.value:null};
        const {data,error}=await c.rpc('nexlab_manage_team_task_v1',args);if(error)throw error;if(!data?.ok)throw new Error('O Supabase não confirmou a alteração da tarefa.');
        state.taskWorkspace=data.workspace||state.taskWorkspace;state.tasksLoaded=true;slot.hidden=true;slot.replaceChildren();renderTeamTasks(state);syncNavigation(state);setTaskMessage(state,editing?'Tarefa atualizada.':'Tarefa criada.','ok');
        try{globalThis.__NEXLAB_PENDING_REALTIME_HUB__?.refresh?.({table:'tasks',eventType:'LOCAL_MUTATION'});globalThis.dispatchEvent(new CustomEvent('nexlab:pending-refresh'));}catch{}
      }catch(error){console.error('NEXLAB team task save',error);setTaskMessage(state,error?.message||'Não foi possível salvar a tarefa.','err');}
      finally{save.disabled=false;cancel.disabled=false;save.textContent=editing?'Salvar alterações':'Criar tarefa';}
    });
    slot.appendChild(form);requestAnimationFrame(()=>title.focus());
  }

  async function completeTeamTask(state,task){
    if(!task?.can_complete||taskWorkspaceReadOnly(state.taskWorkspace))return;
    try{
      const c=await waitClient();const {data,error}=await c.rpc('nexlab_manage_team_task_v1',{p_team_id:state.teamId,p_action:'complete',p_task_id:task.id,p_title:null,p_description:null,p_responsible_id:null,p_deadline:null,p_priority:null,p_status:null});if(error)throw error;if(!data?.ok)throw new Error('O Supabase não confirmou a conclusão da tarefa.');
      state.taskWorkspace=data.workspace||state.taskWorkspace;renderTeamTasks(state);setTaskMessage(state,'Tarefa concluída.','ok');try{globalThis.__NEXLAB_PENDING_REALTIME_HUB__?.refresh?.({table:'tasks',eventType:'LOCAL_MUTATION'});globalThis.dispatchEvent(new CustomEvent('nexlab:pending-refresh'));}catch{}
    }catch(error){console.error('NEXLAB team task complete',error);setTaskMessage(state,error?.message||'Não foi possível concluir a tarefa.','err');}
  }

  async function deleteTeamTask(state,task){
    if(!task?.can_delete||taskWorkspaceReadOnly(state.taskWorkspace))return;if(!confirm(`Excluir a tarefa “${task.title||'Tarefa'}”?`))return;
    try{
      const c=await waitClient();const {data,error}=await c.rpc('nexlab_manage_team_task_v1',{p_team_id:state.teamId,p_action:'delete',p_task_id:task.id,p_title:null,p_description:null,p_responsible_id:null,p_deadline:null,p_priority:null,p_status:null});if(error)throw error;if(!data?.ok)throw new Error('O Supabase não confirmou a exclusão da tarefa.');
      state.taskWorkspace=data.workspace||state.taskWorkspace;renderTeamTasks(state);setTaskMessage(state,'Tarefa excluída.','ok');try{globalThis.__NEXLAB_PENDING_REALTIME_HUB__?.refresh?.({table:'tasks',eventType:'LOCAL_MUTATION'});globalThis.dispatchEvent(new CustomEvent('nexlab:pending-refresh'));}catch{}
    }catch(error){console.error('NEXLAB team task delete',error);setTaskMessage(state,error?.message||'Não foi possível excluir a tarefa.','err');}
  }

  function renderTeamTasks(state){
    const panel=state.tasksPanel;if(!panel)return;
    const list=panel.querySelector('.nexlab-team-task-groups-v02682');
    const create=panel.querySelector('.nexlab-team-task-create-v02682');
    const summary=panel.querySelector('.nexlab-team-task-summary-v02682');
    const filterButton=panel.querySelector('.nexlab-team-task-filter-toggle-v02682');
    const filterPanel=panel.querySelector('.nexlab-team-task-filter-panel-v02682');
    if(!list||!summary)return;
    const workspace=state.taskWorkspace||{};
    const readOnly=taskWorkspaceReadOnly(workspace);
    if(create)create.hidden=!workspace.permissions?.can_create||readOnly;
    if(filterPanel){filterPanel.hidden=!state.taskFilterPanelOpen;filterPanel.querySelectorAll('[data-quick-filter]').forEach(button=>{const active=button.dataset.quickFilter===String(state.taskQuickFilter||'all');button.classList.toggle('is-active',active);button.setAttribute('aria-checked',String(active));});}
    if(filterButton){filterButton.classList.toggle('is-active',String(state.taskQuickFilter||'all')!=='all'||!!clean(state.taskSearchTerm||'',120));filterButton.setAttribute('aria-expanded',String(!!state.taskFilterPanelOpen));}

    const tasks=Array.isArray(workspace.tasks)?workspace.tasks:[];
    const normalized=tasks.map(task=>({...task,status:normalizeTaskStatus(task?.status)}));
    const counts={total:normalized.length,pendente:0,em_andamento:0,concluida:0,cancelada:0};
    normalized.forEach(task=>{counts[normalizeTaskStatus(task.status)]=(counts[normalizeTaskStatus(task.status)]||0)+1;});

    summary.replaceChildren();
    const summaryCards=[
      ['total','Total',counts.total,'todas as tarefas','dot'],
      ['pendente','Pendentes',counts.pendente,taskSummaryLabel('pendente'),'clock'],
      ['em_andamento','Em andamento',counts.em_andamento,taskSummaryLabel('em_andamento'),'play'],
      ['concluida','Concluídas',counts.concluida,taskSummaryLabel('concluida'),'check-circle']
    ];
    for(const [key,label,value,caption,icon] of summaryCards){
      const card=node('article',`nexlab-team-task-stat-card-v02682 is-${key}`);
      const iconShell=node('span','nexlab-team-task-stat-icon-v02682');iconShell.appendChild(teamChatUiIcon(icon,16));
      const copy=node('div','nexlab-team-task-stat-copy-v02682');copy.append(node('strong','',String(value)),node('span','',label),node('small','',caption));
      card.append(iconShell,copy);summary.appendChild(card);
    }

    list.replaceChildren();
    if(state.taskLoadError&&!state.tasksLoaded){const empty=node('div','nexlab-team-task-empty-v02682');empty.append(node('strong','', 'Não foi possível carregar as tarefas'),node('p','',clean(state.taskLoadError?.message||'Tente novamente em instantes.',180)));list.appendChild(empty);return;}
    if(readOnly){const readonly=node('div','nexlab-team-task-readonly-v02682');readonly.append(teamChatUiIcon('clipboard',15),node('span','', 'Equipe arquivada ou em modo somente leitura. As tarefas podem ser consultadas, mas não alteradas.'));list.appendChild(readonly);}
    if(workspace.permissions?.can_view===false){const empty=node('div','nexlab-team-task-empty-v02682');empty.append(node('strong','', 'Sem acesso às tarefas'),node('p','', 'Você não possui permissão para visualizar as tarefas desta equipe.'));list.appendChild(empty);return;}
    if(!normalized.length){const empty=node('div','nexlab-team-task-empty-v02682');empty.append(node('strong','', 'Nenhuma tarefa registrada'),node('p','',workspace.permissions?.can_create?'Use “Nova tarefa” para criar a primeira atividade da equipe.':'Ainda não há tarefas nesta equipe.'));list.appendChild(empty);return;}

    const filtered=normalized.filter(task=>taskMatchesQuickFilter(state,task)&&taskMatchesSearch(task,state.taskSearchTerm));
    if(!filtered.length){const empty=node('div','nexlab-team-task-empty-v02682');empty.append(node('strong','', 'Nenhum resultado encontrado'),node('p','', 'Ajuste a busca ou os filtros para localizar outra tarefa.'));list.appendChild(empty);return;}

    for(const group of taskGroupCatalog()){
      const items=filtered.filter(task=>normalizeTaskStatus(task.status)===group.key);
      if(!items.length&&group.key==='cancelada'&&!counts.cancelada)continue;
      const section=node('section',`nexlab-team-task-group-v02682 is-${taskGroupTone(group.key)}`);
      const header=node('button','nexlab-team-task-group-head-v02682');header.type='button';const groupBodyId=`nexlab-team-task-group-${state.teamId}-${group.key}`;header.setAttribute('aria-expanded',String(!state.taskGroupCollapsed[group.key]));header.setAttribute('aria-controls',groupBodyId);header.addEventListener('click',()=>{state.taskGroupCollapsed[group.key]=!state.taskGroupCollapsed[group.key];renderTeamTasks(state);});
      const headLeft=node('div','nexlab-team-task-group-head-left-v02682');headLeft.append(node('span',`nexlab-team-task-group-dot-v02682 is-${taskGroupTone(group.key)}`),node('strong','',group.label),node('span','nexlab-team-task-group-count-v02682',String(items.length)));
      const headRight=node('span','nexlab-team-task-group-chevron-v02682');headRight.appendChild(teamChatUiIcon('chevron',14));header.append(headLeft,headRight);
      section.appendChild(header);
      const body=node('div','nexlab-team-task-group-body-v02682');body.id=groupBodyId;body.hidden=!!state.taskGroupCollapsed[group.key];
      if(!items.length){const empty=node('div','nexlab-team-task-group-empty-v02682','Nenhuma tarefa neste grupo.');body.appendChild(empty);}else{
        for(const task of items){
          const row=node('article','nexlab-team-task-row-v02682');row.dataset.taskId=String(task.id||'');if(isTaskOverdue(task))row.classList.add('is-overdue');if(normalizeTaskStatus(task.status)==='concluida')row.classList.add('is-done');
          const lead=node(task.can_complete&&!readOnly&&normalizeTaskStatus(task.status)!=='concluida'?'button':'span','nexlab-team-task-row-toggle-v02682');if(task.can_complete&&!readOnly&&normalizeTaskStatus(task.status)!=='concluida'){lead.type='button';lead.title='Concluir tarefa';lead.setAttribute('aria-label',`Concluir tarefa: ${task.title||'Tarefa'}`);lead.addEventListener('click',event=>{event.stopPropagation();completeTeamTask(state,task);});}lead.classList.add(`is-${taskGroupTone(task.status)}`);lead.appendChild(teamChatUiIcon(normalizeTaskStatus(task.status)==='concluida'?'check-circle':'dot',16));
          const main=node('div','nexlab-team-task-row-main-v02682');
          main.append(node('h4','',task.title||'Tarefa'),node('p','',taskMetaLine(task)));
          if(task.description){main.appendChild(node('small','nexlab-team-task-row-description-v02682',task.description));}
          const trailing=node('div','nexlab-team-task-row-trailing-v02682');
          const assignee=node('div','nexlab-team-task-assignee-v02682');const avatar=node('span','nexlab-team-task-assignee-avatar-v02682',initials(task.responsible_name||'U'));const assigneeName=node('span','nexlab-team-task-assignee-name-v02682',clean(task.responsible_name||'Não atribuído',60)||'Não atribuído');assignee.append(avatar,assigneeName);
          const badgeInfo=taskDateBadge(task);const dateBadge=node('div','nexlab-team-task-date-badge-v02682');dateBadge.append(teamChatUiIcon(badgeInfo.icon,14),node('span','',badgeInfo.label||'Sem prazo'));
          const menuWrap=node('div','nexlab-team-task-row-menu-wrap-v02682');
          const toggle=node('button','nexlab-team-task-row-menu-toggle-v02682');toggle.type='button';toggle.setAttribute('aria-label','Ações da tarefa');toggle.setAttribute('aria-haspopup','menu');toggle.setAttribute('aria-expanded',String(state.taskOpenMenuId===String(task.id||'')));toggle.appendChild(teamChatUiIcon('more',16));toggle.addEventListener('click',event=>{event.stopPropagation();const taskId=String(task.id||'');const opening=state.taskOpenMenuId!==taskId;state.taskOpenMenuId=opening?taskId:'';renderTeamTasks(state);requestAnimationFrame(()=>opening?focusTaskMenu(state,taskId):focusTaskMenuToggle(state,taskId));});
          menuWrap.appendChild(toggle);
          const menu=node('div','nexlab-team-task-row-menu-v02682');menu.hidden=state.taskOpenMenuId!==String(task.id||'');menu.setAttribute('role','menu');
          const details=node('button','','Ver detalhes');details.type='button';details.setAttribute('role','menuitem');details.addEventListener('click',event=>{event.stopPropagation();showTaskDetails(state,task);});menu.appendChild(details);
          if(task.can_complete&&normalizeTaskStatus(task.status)!=='concluida'&&!readOnly){const done=node('button','is-complete','Concluir');done.type='button';done.setAttribute('role','menuitem');done.addEventListener('click',event=>{event.stopPropagation();completeTeamTask(state,task);});menu.appendChild(done);}
          if(task.can_edit&&!readOnly){const statusAction=node('button','',taskStatusActionLabel(task.status));statusAction.type='button';statusAction.setAttribute('role','menuitem');statusAction.addEventListener('click',event=>{event.stopPropagation();updateTeamTaskStatus(state,task,taskNextStatus(task.status));});menu.appendChild(statusAction);}
          if(task.can_edit&&!readOnly){const reassign=node('button','','Reatribuir responsável');reassign.type='button';reassign.setAttribute('role','menuitem');reassign.addEventListener('click',event=>{event.stopPropagation();state.taskOpenMenuId='';showTaskForm(state,task);requestAnimationFrame(()=>state.tasksPanel?.querySelector('.nexlab-team-task-form-v02682 select')?.focus());});menu.appendChild(reassign);}
          if(task.can_edit&&!readOnly){const edit=node('button','','Editar');edit.type='button';edit.setAttribute('role','menuitem');edit.addEventListener('click',event=>{event.stopPropagation();state.taskOpenMenuId='';showTaskForm(state,task);});menu.appendChild(edit);}
          if(task.can_delete&&!readOnly){const remove=node('button','is-danger','Excluir');remove.type='button';remove.setAttribute('role','menuitem');remove.addEventListener('click',event=>{event.stopPropagation();state.taskOpenMenuId='';deleteTeamTask(state,task);});menu.appendChild(remove);}
          if(menu.childElementCount)menuWrap.appendChild(menu);
          trailing.append(assignee,dateBadge,menuWrap);
          row.append(lead,main,trailing);body.appendChild(row);
        }
      }
      section.appendChild(body);list.appendChild(section);
    }
    if(state.targetTaskId)highlightTaskTarget(state,state.targetTaskId);
  }

  function highlightTaskTarget(state,id){
    if(!id||!state.tasksPanel)return;const target=state.tasksPanel.querySelector(`[data-task-id="${CSS.escape(String(id))}"]`);if(!target)return;
    target.classList.add('is-target');target.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>target.classList.remove('is-target'),3200);state.targetTaskId='';
  }

  function setInlineMessage(state,message,type='info'){
    const box=state.chatPanel?.querySelector('.nexlab-team-chat-notice-rebuilt-v02682');if(!box)return;
    box.hidden=!message;box.className=`nexlab-team-chat-notice-rebuilt-v02682 is-${type}`;box.textContent=message||'';
  }

  function setCommunicationState(state,status,detail=''){
    const context=state.chatPanel?.querySelector('.nexlab-team-chat-context-rebuilt-v02682');
    const labels={
      checking:['Verificando conexão','Consultando o serviço de mensagens.'],
      connected:['Equipe conectada','Mensagens sincronizadas normalmente.'],
      reconnecting:['Reconectando','Tentando restabelecer o serviço de mensagens.'],
      offline:['Sem conexão','O dispositivo está offline.'],
      error:['Comunicação indisponível','Não foi possível sincronizar a conversa.']
    };
    const safe=labels[status]?status:'checking';
    state.communication=state.communication||{};state.communication.status=safe;
    if(!context)return;
    context.dataset.connectionState=safe;context.className=`nexlab-team-chat-context-rebuilt-v02682 is-${safe}`;
    const strong=context.querySelector('strong'),span=context.querySelector('span:not(.nexlab-team-chat-context-dot-rebuilt-v02682)');
    if(strong)strong.textContent=labels[safe][0];if(span)span.textContent=detail||labels[safe][1];
  }

  function markCommunicationSuccess(state,startedAt){
    const now=Date.now(),latency=Math.max(0,Math.round(performance.now()-startedAt));state.communication={...(state.communication||{}),status:'connected',lastSuccessAt:now,lastLatencyMs:latency,lastErrorAt:null};
    const time=formatTime(now);setCommunicationState(state,'connected',`Última sincronização às ${time} • ${latency} ms.`);
  }

  function markCommunicationFailure(state,error){
    const offline=navigator.onLine===false;state.communication={...(state.communication||{}),status:offline?'offline':'error',lastErrorAt:Date.now()};
    setCommunicationState(state,offline?'offline':'error',offline?'O dispositivo está offline.':clean(error?.message||'Não foi possível sincronizar a conversa.',180));
  }

  function syncNavigation(state){
    const nav=state.panel.querySelector('.nexlab-team-tabs-rebuilt-v02682');if(!nav)return;
    const chat=nav.querySelector('[data-team-tab="conversation"]');
    if(chat){const denied=state.access?.can_view_chat===false;chat.disabled=denied;chat.title=denied?'Você não possui acesso à conversa desta equipe.':'';}
    const tasks=nav.querySelector('[data-team-tab="tasks"]');
    if(tasks){const denied=state.taskWorkspace?.permissions?.can_view===false;tasks.disabled=denied;tasks.title=denied?'Você não possui acesso às tarefas desta equipe.':'';}
    for(const button of nav.querySelectorAll('[data-team-tab]')){const active=button.dataset.teamTab===state.activeTab;button.classList.toggle('is-active',active);button.setAttribute('aria-selected',String(active));}
  }

  function classifyAndApply(state){
    const content=state.panel.querySelector(CONTENT_SELECTOR);state.content=content||null;if(!content)return;
    const workspace=state.workspace||state.panel.querySelector(':scope > .nexlab-team-workspace-v057');
    const footer=state.panel.querySelector(':scope > .team-details-v2680__footer');state.footer=footer||null;
    const groups={
      overview:[...content.querySelectorAll(':scope > .team-details-v2680__summary, :scope > .team-details-v2680__members, :scope > .team-workspace-v2680__history')],
      links:[...content.querySelectorAll(':scope > .nexlab-team-links-rebuilt-v02682')]
    };
    // Fallback for older DOM shapes.
    if(!groups.overview.length){for(const selector of ['.team-details-v2680__summary','.team-details-v2680__members','.team-workspace-v2680__history']){const el=content.querySelector(selector);if(el)groups.overview.push(el);}}
    if(!groups.links.length){const el=content.querySelector('.nexlab-team-links-rebuilt-v02682');if(el)groups.links.push(el);}
    const managed=new Set([...groups.overview,...groups.links]);
    for(const el of managed){el.hidden=true;}
    const workspaceTab=state.activeTab==='conversation'||state.activeTab==='tasks';
    content.hidden=workspaceTab;
    if(footer)footer.hidden=workspaceTab;
    if(workspace)workspace.hidden=!workspaceTab;
    if(state.chatPanel)state.chatPanel.hidden=state.activeTab!=='conversation';
    if(state.tasksPanel)state.tasksPanel.hidden=state.activeTab!=='tasks';
    state.panel.classList.toggle('is-nexlab-team-workspace-v057',workspaceTab);
    state.panel.classList.toggle('is-nexlab-team-tasks-scroll-v02682',state.activeTab==='tasks');
    if(!workspaceTab){for(const el of groups[state.activeTab]||groups.overview)el.hidden=false;}
    syncNavigation(state);
  }

  function activateTab(state,key){
    if(!['overview','tasks','links','conversation'].includes(key))key='overview';
    if(key==='conversation'&&state.access?.can_view_chat===false)return;
    if(key==='tasks'&&state.taskWorkspace?.permissions?.can_view===false)return;
    state.activeTab=key;classifyAndApply(state);
    if(key==='conversation')void ensureConversationReady(state);
    if(key==='tasks')void ensureTeamTasksReady(state);
  }

  async function ensureConversationReady(state){
    await loadAccess(state);
    if(state.access?.can_view_chat===false){setInlineMessage(state,'Você não possui acesso à conversa desta equipe.','err');return;}
    const lastSync=Number(state.communication?.lastSuccessAt||0);const stale=state.loaded&&lastSync>0&&Date.now()-lastSync>60000;
    if((!state.loaded||stale)&&!state.loading)await loadMessages(state,{force:state.loaded});
    else if(state.loaded&&state.communication?.status!=='connected'&&navigator.onLine!==false)setCommunicationState(state,'connected',lastSync?`Última sincronização às ${formatTime(lastSync)}.`:'Conversa carregada.');
    if(state.targetContentId)highlightTarget(state,state.targetContentId);
  }

  async function loadMessages(state,{force=false}={}){
    if(state.loading)return;state.loading=true;
    const list=state.chatPanel.querySelector('.nexlab-team-chat-list-rebuilt-v02682');const refresh=state.chatPanel.querySelector('.nexlab-team-chat-refresh-rebuilt-v02682');
    refresh.disabled=true;refresh.classList.add('is-loading');refresh.setAttribute('aria-busy','true');if(!state.loaded||force){list.classList.add('is-loading');setInlineMessage(state,'Carregando conversa...','info');}setCommunicationState(state,state.loaded?'reconnecting':'checking');const startedAt=performance.now();
    try{
      const data=await api(`/v1/teams/${encodeURIComponent(state.teamId)}/messages?limit=${PAGE_SIZE}`);
      state.messages=Array.isArray(data?.messages)?data.messages:[];state.nextCursor=data?.next_cursor||null;state.loaded=true;markCommunicationSuccess(state,startedAt);setInlineMessage(state,'','info');renderMessages(state);syncOlderButton(state);scrollToBottom(state,force?'auto':'instant');if(state.targetContentId)highlightTarget(state,state.targetContentId);
    }catch(error){console.error('NEXLAB team chat load',error);markCommunicationFailure(state,error);setInlineMessage(state,error.message||'Não foi possível carregar a conversa.','err');}
    finally{state.loading=false;list.classList.remove('is-loading');refresh.disabled=false;refresh.classList.remove('is-loading');refresh.removeAttribute('aria-busy');syncOlderButton(state);}
  }

  async function loadOlder(state){
    if(state.loading||!state.nextCursor)return;state.loading=true;
    const older=state.chatPanel.querySelector('.nexlab-team-chat-older-rebuilt-v02682');const list=state.chatPanel.querySelector('.nexlab-team-chat-list-rebuilt-v02682');const beforeHeight=list.scrollHeight;older.disabled=true;older.textContent='Carregando...';
    const startedAt=performance.now();
    try{
      const data=await api(`/v1/teams/${encodeURIComponent(state.teamId)}/messages?limit=${PAGE_SIZE}&cursor=${encodeURIComponent(state.nextCursor)}`);
      const rows=Array.isArray(data?.messages)?data.messages:[];const known=new Set(state.messages.map(row=>String(row.id)));state.messages=[...rows.filter(row=>!known.has(String(row.id))),...state.messages];state.nextCursor=data?.next_cursor||null;markCommunicationSuccess(state,startedAt);renderMessages(state);requestAnimationFrame(()=>{list.scrollTop=Math.max(0,list.scrollHeight-beforeHeight);});
    }catch(error){markCommunicationFailure(state,error);setInlineMessage(state,error.message||'Não foi possível carregar mensagens anteriores.','err');}
    finally{state.loading=false;older.disabled=false;older.textContent='Carregar mensagens anteriores';syncOlderButton(state);}
  }

  function syncOlderButton(state){const older=state.chatPanel?.querySelector('.nexlab-team-chat-older-rebuilt-v02682');if(older)older.hidden=!state.nextCursor;}

  function appendTextWithMentions(container,text){
    const source=String(text||'');const regex=/@[\p{L}\p{N}_.-]+/gu;let index=0,match;
    while((match=regex.exec(source))){if(match.index>index)container.appendChild(document.createTextNode(source.slice(index,match.index)));container.appendChild(node('span','nexlab-team-inline-mention-rebuilt-v02682',match[0]));index=match.index+match[0].length;}
    if(index<source.length)container.appendChild(document.createTextNode(source.slice(index)));
  }

  function compactMessagePreview(value,max=180){return clean(String(value||'').replace(/\s+/g,' '),max);}
  function parseMessageBody(value){const source=String(value||'');const match=source.match(/^\[↪ ([^\]\n]{1,80})\] ([^\n]{1,220})\n\n([\s\S]*)$/u);return match?{reply:{author:match[1],preview:match[2]},body:match[3]}:{reply:null,body:source};}
  function composeReplyMessage(text,reply){if(!reply)return text;const author=clean(String(reply.authorName||'Usuário').replace(/[\]\n]/g,' '),80)||'Usuário';const preview=compactMessagePreview(reply.preview,180)||'Mensagem';return `[↪ ${author}] ${preview}\n\n${text}`;}
  async function copyMessageText(value){const text=String(value||'');try{if(navigator.clipboard?.writeText){await navigator.clipboard.writeText(text);return true;}}catch{}try{const area=document.createElement('textarea');area.value=text;area.setAttribute('readonly','');area.style.position='fixed';area.style.opacity='0';document.body.appendChild(area);area.select();const ok=document.execCommand('copy');area.remove();return ok;}catch{return false;}}
  function createTaskFromMessage(state,message){const author=profile(state,message.author_id);const parsed=parseMessageBody(message.message);const body=compactMessagePreview(parsed.body,160);activateTab(state,'tasks');void ensureTeamTasksReady(state).then(()=>showTaskForm(state,null,{title:`Acompanhar mensagem de ${clean(author?.nome||'integrante',70)}`,description:`Criada a partir da conversa da equipe em ${formatDate(message.created_at)}.\n\n${body}`}));}
  function messageActionButton(icon,label,onClick){const button=node('button','nexlab-team-chat-message-action-v02682');button.type='button';button.title=label;button.setAttribute('aria-label',label);button.append(teamChatUiIcon(icon,14),node('span','',label));button.addEventListener('click',event=>{event.stopPropagation();onClick();});return button;}

  function renderMessages(state){
    const list=state.chatPanel.querySelector('.nexlab-team-chat-list-rebuilt-v02682');list.replaceChildren();
    if(!state.messages.length){const empty=node('div','nexlab-team-chat-empty-rebuilt-v02682');const icon=node('span','nexlab-team-chat-empty-icon-rebuilt-v02682');icon.appendChild(teamChatUiIcon('conversation',27));empty.append(icon,node('strong','', 'Nenhuma mensagem ainda'),node('p','',state.access?.can_send_message?'Comece a conversa com sua equipe.':'Ainda não há mensagens registradas nesta conversa.'));list.appendChild(empty);return;}
    let lastAuthor='',lastDay='';
    for(const message of state.messages){
      const currentDay=chatDayKey(message.created_at);
      if(currentDay!==lastDay){const separator=node('div','nexlab-team-chat-day-rebuilt-v02682',chatDayLabel(message.created_at));separator.setAttribute('role','separator');list.appendChild(separator);lastAuthor='';lastDay=currentDay;}
      const authorId=String(message.author_id||'');const own=authorId===state.currentUserId;const grouped=lastAuthor===authorId;
      const author=profile(state,message.author_id);const row=node('article',`nexlab-team-chat-row-rebuilt-v02682 ${own?'is-own':'is-other'}${grouped?' is-grouped-rebuilt-v02682':''}`);row.dataset.messageId=String(message.id||'');
      if(!own){const avatar=node('span',`nexlab-team-chat-avatar-rebuilt-v02682${grouped?' is-ghost-rebuilt-v02682':''}`,initials(author?.nome));avatar.title=author?.nome||'Usuário';row.appendChild(avatar);}
      const wrap=node('div','nexlab-team-chat-bubble-wrap-rebuilt-v02682');
      const meta=node('div','nexlab-team-chat-meta-rebuilt-v02682');
      if(own){meta.append(node('strong','', 'Você'),node('time','',formatTime(message.created_at)));}
      else if(!grouped){meta.append(node('strong','',author?.nome||'Usuário'),node('time','',formatTime(message.created_at)));meta.title=roleLabel(author?.role);}
      if(meta.childNodes.length)wrap.appendChild(meta);
      const bubble=node('div','nexlab-team-chat-bubble-rebuilt-v02682');const parsed=parseMessageBody(message.message);
      if(parsed.reply){const quote=node('div','nexlab-team-chat-reply-quote-v02682');quote.append(node('strong','',`Respondendo a ${parsed.reply.author}`),node('span','',parsed.reply.preview));bubble.appendChild(quote);}
      const text=node('p','nexlab-team-chat-text-rebuilt-v02682');appendTextWithMentions(text,parsed.body);bubble.appendChild(text);
      if(Array.isArray(message.mentions)&&message.mentions.length){const mentions=node('div','nexlab-team-chat-mentions-rebuilt-v02682');for(const id of message.mentions)mentions.appendChild(node('span','',`@ ${profileName(state,id)}`));bubble.appendChild(mentions);}
      wrap.appendChild(bubble);
      const actionBar=node('div','nexlab-team-chat-message-actions-v02682');
      if(!own&&state.access?.can_send_message!==false){actionBar.append(messageActionButton('reply','Responder',()=>state.composerApi?.replyToMessage?.(message)),messageActionButton('at','Mencionar',()=>state.composerApi?.mentionUser?.(authorId)));}
      actionBar.append(messageActionButton('tasks','Criar pendência',()=>createTaskFromMessage(state,message)),messageActionButton('clipboard','Copiar mensagem',async()=>{const ok=await copyMessageText(parsed.body);toast(ok?'Mensagem copiada.':'Não foi possível copiar a mensagem.',ok?'ok':'err');}));
      const more=node('button','nexlab-team-chat-message-more-v02682');more.type='button';more.title='Ações da mensagem';more.setAttribute('aria-label','Ações da mensagem');more.setAttribute('aria-expanded','false');more.appendChild(teamChatUiIcon('more',16));more.addEventListener('click',event=>{event.stopPropagation();const open=!row.classList.contains('is-actions-open-v02682');for(const item of list.querySelectorAll('.is-actions-open-v02682'))if(item!==row){item.classList.remove('is-actions-open-v02682');item.querySelector('.nexlab-team-chat-message-more-v02682')?.setAttribute('aria-expanded','false');}row.classList.toggle('is-actions-open-v02682',open);more.setAttribute('aria-expanded',String(open));});
      wrap.append(actionBar,more);row.appendChild(wrap);list.appendChild(row);lastAuthor=authorId;
    }
  }

  function scrollToBottom(state,behavior='smooth'){const list=state.chatPanel?.querySelector('.nexlab-team-chat-list-rebuilt-v02682');if(list)requestAnimationFrame(()=>list.scrollTo({top:list.scrollHeight,behavior:behavior==='instant'?'auto':behavior}));}

  function mentionChips(state,selected,onRemove){const wrap=node('div','nexlab-team-composer-chips-rebuilt-v02682');for(const id of selected){const chip=node('span','nexlab-team-mention-chip-rebuilt-v02682');chip.append(document.createTextNode(`@ ${profileName(state,id)}`));const remove=node('button','','×');remove.type='button';remove.setAttribute('role','menuitem');remove.setAttribute('aria-label',`Remover menção a ${profileName(state,id)}`);remove.addEventListener('click',()=>onRemove(id));chip.appendChild(remove);wrap.appendChild(chip);}return wrap;}

  function buildComposer(state){
    const wrap=node('div','nexlab-team-composer-rebuilt-v02682');
    const tools=node('div','nexlab-team-composer-tools-rebuilt-v02682');const plus=node('button','nexlab-team-composer-plus-rebuilt-v02682','+');plus.type='button';plus.setAttribute('aria-label','Opções da mensagem');plus.setAttribute('aria-expanded','false');
    const menu=node('div','nexlab-team-composer-menu-rebuilt-v02682');menu.hidden=true;const taskAction=node('button','','Criar tarefa da equipe');taskAction.type='button';const mentionAction=node('button','','Mencionar alguém');mentionAction.type='button';menu.append(taskAction,mentionAction);tools.append(plus,menu);
    const inputWrap=node('div','nexlab-team-composer-input-rebuilt-v02682');const textarea=node('textarea','');textarea.rows=1;textarea.maxLength=4000;textarea.placeholder='Escreva uma mensagem...';textarea.setAttribute('aria-label','Mensagem para a equipe');
    const mentionButton=node('button','nexlab-team-composer-mention-rebuilt-v02682');mentionButton.type='button';mentionButton.setAttribute('aria-label','Mencionar alguém');mentionButton.title='Mencionar alguém';mentionButton.appendChild(teamChatUiIcon('at',17));
    const suggestions=node('div','nexlab-team-mention-suggestions-rebuilt-v02682');suggestions.hidden=true;inputWrap.append(textarea,mentionButton,suggestions);
    const send=node('button','nexlab-team-composer-send-rebuilt-v02682');send.type='button';send.append(node('span','', 'Enviar'),teamChatUiIcon('send',15));
    const selected=new Set();let replyTarget=null;const replySlot=node('div','nexlab-team-composer-reply-rebuilt-v02682');replySlot.hidden=true;const chipsSlot=node('div','nexlab-team-composer-selected-rebuilt-v02682');const counter=node('span','nexlab-team-composer-counter-rebuilt-v02682','0/4000');

    const refreshChips=()=>{chipsSlot.replaceChildren();if(selected.size)chipsSlot.appendChild(mentionChips(state,[...selected],id=>{selected.delete(String(id));refreshChips();}));};
    const refreshReply=()=>{replySlot.replaceChildren();replySlot.hidden=!replyTarget;if(!replyTarget)return;const copy=node('div','');copy.append(node('strong','',`Respondendo a ${replyTarget.authorName}`),node('span','',replyTarget.preview));const close=node('button','','×');close.type='button';close.setAttribute('aria-label','Cancelar resposta');close.addEventListener('click',()=>{replyTarget=null;refreshReply();});replySlot.append(copy,close);};
    const insertMention=id=>{const row=profile(state,id);if(!row||String(id)===state.currentUserId)return;const first=clean(row.nome||'Usuário',80).split(/\s+/)[0];const token='@'+first;if(!textarea.value.includes(token)){const prefix=textarea.value&& !/\s$/.test(textarea.value)?' ':'';textarea.value+=prefix+token+' ';textarea.dispatchEvent(new Event('input',{bubbles:true}));}selected.add(String(id));refreshChips();textarea.focus();textarea.setSelectionRange(textarea.value.length,textarea.value.length);};
    state.composerApi={focus:()=>textarea.focus(),mentionUser:id=>insertMention(String(id)),replyToMessage:message=>{const authorId=String(message?.author_id||'');const author=profile(state,authorId);replyTarget={authorId,authorName:author?.nome||'Usuário',preview:compactMessagePreview(parseMessageBody(message?.message).body,180)};if(authorId&&authorId!==state.currentUserId)selected.add(authorId);refreshChips();refreshReply();textarea.focus();},clearReply:()=>{replyTarget=null;refreshReply();}};
    const hideSuggestions=()=>{suggestions.hidden=true;suggestions.replaceChildren();};
    const suggestionQuery=()=>{const before=textarea.value.slice(0,textarea.selectionStart??textarea.value.length);const match=before.match(/(?:^|\s)@([^\s@]{0,40})$/u);return match?{query:match[1],start:before.length-match[1].length-1,end:before.length}:null;};
    const showSuggestions=(force=false)=>{
      let info=suggestionQuery();if(force&&!info){const pos=textarea.selectionStart??textarea.value.length;const before=textarea.value.slice(0,pos);const prefix=before&& !/\s$/.test(before)?' ':'';textarea.setRangeText(prefix+'@',pos,pos,'end');info=suggestionQuery();}
      if(!info){hideSuggestions();return;}
      const q=info.query.toLocaleLowerCase('pt-BR');const rows=[...(state.profiles?.values?.()||[])].filter(row=>row?.id&&String(row.id)!==state.currentUserId&&false!==row.ativo&&clean(row.nome,100).toLocaleLowerCase('pt-BR').includes(q)).slice(0,7);
      suggestions.replaceChildren();if(!rows.length){suggestions.appendChild(node('p','', 'Nenhuma pessoa encontrada.'));suggestions.hidden=false;return;}
      for(const row of rows){const button=node('button','',row.nome||'Usuário');button.type='button';button.appendChild(node('small','',roleLabel(row.role)));button.addEventListener('click',()=>{const token='@'+clean(row.nome,80).split(/\s+/)[0];const value=textarea.value;textarea.value=value.slice(0,info.start)+token+' '+value.slice(info.end);selected.add(String(row.id));refreshChips();hideSuggestions();textarea.focus();const pos=info.start+token.length+1;textarea.setSelectionRange(pos,pos);counter.textContent=`${textarea.value.length}/4000`;});suggestions.appendChild(button);}
      suggestions.hidden=false;
    };

    plus.addEventListener('click',()=>{menu.hidden=!menu.hidden;plus.setAttribute('aria-expanded',String(!menu.hidden));});
    taskAction.addEventListener('click',()=>{menu.hidden=true;plus.setAttribute('aria-expanded','false');activateTab(state,'tasks');void ensureTeamTasksReady(state).then(()=>showTaskForm(state));});
    mentionAction.addEventListener('click',()=>{menu.hidden=true;plus.setAttribute('aria-expanded','false');textarea.focus();showSuggestions(true);});
    mentionButton.addEventListener('click',()=>{textarea.focus();showSuggestions(true);});
    textarea.addEventListener('input',()=>{counter.textContent=`${textarea.value.length}/4000`;textarea.style.height='auto';textarea.style.height=`${Math.min(textarea.scrollHeight,104)}px`;if(!textarea.value.includes('@')&&selected.size){selected.clear();refreshChips();}showSuggestions();});
    textarea.addEventListener('keyup',event=>{if(['ArrowLeft','ArrowRight','Home','End'].includes(event.key))showSuggestions();});
    textarea.addEventListener('keydown',event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();send.click();}});
    textarea.addEventListener('blur',()=>setTimeout(hideSuggestions,180));

    send.addEventListener('click',async()=>{
      const text=textarea.value.trim();if(!text){textarea.focus();return;}const outgoing=composeReplyMessage(text,replyTarget);if(outgoing.length>4000){setInlineMessage(state,'A resposta ficou longa demais. Reduza o texto antes de enviar.','err');textarea.focus();return;}textarea.disabled=send.disabled=plus.disabled=mentionButton.disabled=true;send.querySelector('span').textContent='Enviando...';const startedAt=performance.now();
      try{
        const data=await api(`/v1/teams/${encodeURIComponent(state.teamId)}/messages`,{method:'POST',body:JSON.stringify({message:outgoing,mentions:[...selected]})});
        if(data?.message){state.messages.push(data.message);markCommunicationSuccess(state,startedAt);renderMessages(state);textarea.value='';textarea.style.height='';selected.clear();replyTarget=null;refreshChips();refreshReply();counter.textContent='0/4000';setInlineMessage(state,'','info');scrollToBottom(state);}
      }catch(error){markCommunicationFailure(state,error);setInlineMessage(state,error.message||'Não foi possível enviar a mensagem.','err');textarea.focus();}
      finally{textarea.disabled=send.disabled=plus.disabled=mentionButton.disabled=false;send.querySelector('span').textContent='Enviar';}
    });

    const inputRow=node('div','nexlab-team-composer-row-rebuilt-v02682');inputRow.append(tools,inputWrap,send);wrap.append(replySlot,chipsSlot,inputRow,counter);return wrap;
  }

  function renderComposer(state){
    const slot=state.chatPanel?.querySelector('.nexlab-team-chat-composer-slot-rebuilt-v02682');if(!slot)return;slot.replaceChildren();state.composerApi=null;
    if(state.accessError){slot.appendChild(node('p','nexlab-team-chat-readonly-rebuilt-v02682','Não foi possível verificar sua permissão para enviar mensagens.'));return;}
    if(!state.access?.can_send_message){slot.appendChild(node('p','nexlab-team-chat-readonly-rebuilt-v02682','Você pode acompanhar a conversa, mas não possui permissão para enviar mensagens nesta equipe.'));return;}
    slot.appendChild(buildComposer(state));
  }

  function highlightTarget(state,id){
    if(!id)return;const target=state.chatPanel.querySelector(`[data-message-id="${CSS.escape(id)}"]`);if(!target)return;
    target.classList.add('is-target');target.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>target.classList.remove('is-target'),3200);state.targetContentId='';
  }

  async function resolveNotificationTarget(){
    if(notificationPromise)return notificationPromise;
    notificationPromise=(async()=>{
      let notificationId='';try{notificationId=new URL(location.href).searchParams.get('notification')||'';}catch{}
      if(!isUuid(notificationId)){try{const stored=JSON.parse(sessionStorage.getItem('nexlabNotificationTarget')||'null');notificationId=String(stored?.notificationId||stored?.notification_id||'');}catch{}}
      if(isUuid(notificationId)){
        const c=await waitClient();const {data,error}=await c.from('notifications').select('id,target_tab,entity_type,entity_id,metadata').eq('id',notificationId).maybeSingle();
        if(!error&&data&&String(data.entity_type||'')==='team'&&isUuid(data.entity_id)&&['conversation','tasks'].includes(String(data.metadata?.open_section||''))){
          const section=String(data.metadata.open_section);return {notificationId,teamId:String(data.entity_id),section,contentId:section==='conversation'?String(data.metadata?.communication_content_id||''):'',taskId:section==='tasks'?String(data.metadata?.task_id||''):''};
        }
      }
      try{
        const direct=JSON.parse(sessionStorage.getItem('nexlabTeamTaskTarget')||'null');
        if(isUuid(direct?.teamId)&&isUuid(direct?.taskId)){sessionStorage.removeItem('nexlabTeamTaskTarget');return {notificationId:'',teamId:String(direct.teamId),section:'tasks',contentId:'',taskId:String(direct.taskId)};}
      }catch{}
      return null;
    })().catch(()=>null).then(value=>{pendingNotification=value;return value;});
    return notificationPromise;
  }

  function tryOpenPendingTeam(){
    if(!pendingNotification?.teamId||document.querySelector(PANEL_SELECTOR))return;
    const card=document.querySelector(`[data-nexlab-record-id="${CSS.escape(pendingNotification.teamId)}"]`);if(!card)return;
    const buttons=[...card.querySelectorAll('button')];const details=buttons.find(button=>/^(ver detalhes|detalhes)$/i.test(clean(button.textContent,60)));if(details){details.click();}
  }

  function mountWorkspace(panel,state){
    panel.querySelectorAll('.nexlab-team-workspace-v057').forEach(el=>el.remove());
    // Remove leftovers from the previous injected layout, including panels nested in the original content.
    panel.querySelectorAll('.nexlab-team-tasks-rebuilt-v02682,.nexlab-team-chat-rebuilt-v02682').forEach(el=>el.remove());
    const tasks=buildTasksPanel(state);const chat=buildConversationPanel(state);const workspace=buildTeamWorkspace(tasks,chat);
    state.tasksPanel=tasks;state.chatPanel=chat;state.workspace=workspace;state.content=panel.querySelector(CONTENT_SELECTOR);state.footer=panel.querySelector(':scope > .team-details-v2680__footer');
    const nav=panel.querySelector(':scope > .nexlab-team-tabs-rebuilt-v02682');
    if(nav)nav.insertAdjacentElement('afterend',workspace);else if(state.content)state.content.insertAdjacentElement('beforebegin',workspace);else panel.appendChild(workspace);
    if(state.loaded){renderMessages(state);syncOlderButton(state);}renderComposer(state);setCommunicationState(state,state.communication?.status||'checking',state.communication?.status==='connected'&&state.communication?.lastSuccessAt?`Última sincronização às ${formatTime(state.communication.lastSuccessAt)}${state.communication.lastLatencyMs!=null?` • ${state.communication.lastLatencyMs} ms.`:'.'}`:'');
    if(state.tasksLoaded)renderTeamTasks(state);
  }

  function createState(panel,teamId){
    const state={panel,teamId,activeTab:'overview',messages:[],nextCursor:null,loaded:false,loading:false,access:null,accessError:null,accessLoading:null,profiles:new Map(),currentUserId:'',targetContentId:'',targetTaskId:'',content:null,footer:null,workspace:null,chatPanel:null,tasksPanel:null,taskWorkspace:null,tasksLoaded:false,tasksLoading:null,taskRealtimeUnsubscribe:null,taskRealtimeTimer:null,communication:{status:navigator.onLine===false?'offline':'checking',lastSuccessAt:null,lastLatencyMs:null,lastErrorAt:null},composerApi:null,taskSearchTerm:'',taskQuickFilter:'all',taskFilterPanelOpen:false,taskGroupCollapsed:{pendente:false,em_andamento:false,concluida:false,cancelada:false},taskOpenMenuId:'',taskDetailId:'',taskLoadError:null};
    panel.classList.add('has-nexlab-team-chat-rebuilt-v02682','has-nexlab-team-tasks-rebuilt-v02682');
    const header=panel.querySelector(HEADER_SELECTOR);const nav=buildTabs(state);
    if(header)header.insertAdjacentElement('afterend',nav);else panel.prepend(nav);
    mountWorkspace(panel,state);
    states.set(panel,state);stateByTeamId.set(teamId,state);bindTaskRealtime(state);
    if(pendingNotification?.teamId===teamId){state.activeTab=pendingNotification.section||'overview';state.targetContentId=pendingNotification.contentId||'';state.targetTaskId=pendingNotification.taskId||'';}
    void loadAccess(state).then(()=>{classifyAndApply(state);if(state.activeTab==='conversation')void ensureConversationReady(state);if(state.activeTab==='tasks')void ensureTeamTasksReady(state);});classifyAndApply(state);return state;
  }

  function rebindState(panel,state){
    state.panel=panel;state.content=panel.querySelector(CONTENT_SELECTOR);state.footer=panel.querySelector(':scope > .team-details-v2680__footer');
    panel.classList.add('has-nexlab-team-chat-rebuilt-v02682','has-nexlab-team-tasks-rebuilt-v02682');
    panel.querySelectorAll('.nexlab-team-tabs-rebuilt-v02682,.nexlab-team-workspace-v057,.nexlab-team-tasks-rebuilt-v02682,.nexlab-team-chat-rebuilt-v02682').forEach(el=>el.remove());
    const header=panel.querySelector(HEADER_SELECTOR);const nav=buildTabs(state);
    if(header)header.insertAdjacentElement('afterend',nav);else panel.prepend(nav);
    mountWorkspace(panel,state);
    states.set(panel,state);bindTaskRealtime(state);classifyAndApply(state);
    if(state.activeTab==='conversation'&&!state.loaded)void ensureConversationReady(state);
    if(state.activeTab==='tasks'&&!state.tasksLoaded)void ensureTeamTasksReady(state);
    return state;
  }

  function ensurePanel(panel){
    if(!(panel instanceof HTMLElement))return;const teamId=String(panel.dataset.nexlabTeamId||'');if(!isUuid(teamId))return;
    let state=states.get(panel);if(!state||state.teamId!==teamId){
      panel.querySelectorAll('.nexlab-team-tabs-rebuilt-v02682,.nexlab-team-workspace-v057,.nexlab-team-tasks-rebuilt-v02682,.nexlab-team-chat-rebuilt-v02682').forEach(el=>el.remove());
      const cached=stateByTeamId.get(teamId);
      state=cached&&!cached.panel?.isConnected?rebindState(panel,cached):createState(panel,teamId);
    }else{
      let nav=panel.querySelector(':scope > .nexlab-team-tabs-rebuilt-v02682');
      if(!nav){nav=buildTabs(state);const header=panel.querySelector(HEADER_SELECTOR);if(header)header.insertAdjacentElement('afterend',nav);else panel.prepend(nav);}
      const workspace=panel.querySelector(':scope > .nexlab-team-workspace-v057');
      if(!workspace||!workspace.querySelector('.nexlab-team-tasks-rebuilt-v02682')||!workspace.querySelector('.nexlab-team-chat-rebuilt-v02682'))mountWorkspace(panel,state);
      classifyAndApply(state);
    }
  }

  function closeTaskFloatingPanels(target,{escape=false}={}){
    for(const state of stateByTeamId.values()){
      if(state.activeTab!=='tasks'||!state.tasksPanel?.isConnected)continue;
      let changed=false;const menuReturnId=state.taskOpenMenuId;
      if(state.taskFilterPanelOpen&&(escape||!(target instanceof Element)||!target.closest('.nexlab-team-task-filters-wrap-v02682'))){state.taskFilterPanelOpen=false;changed=true;}
      if(state.taskOpenMenuId&&(escape||!(target instanceof Element)||!target.closest('.nexlab-team-task-row-menu-wrap-v02682'))){state.taskOpenMenuId='';changed=true;}
      if(changed)renderTeamTasks(state);
      if(escape&&menuReturnId)requestAnimationFrame(()=>focusTaskMenuToggle(state,menuReturnId));
      if(escape&&state.taskDetailId)closeTaskDetail(state);
    }
  }

  function scan(){observerScheduled=false;tryOpenPendingTeam();document.querySelectorAll(PANEL_SELECTOR).forEach(ensurePanel);}
  function scheduleScan(){if(observerScheduled)return;observerScheduled=true;requestAnimationFrame(scan);}
  const observer=new MutationObserver(scheduleScan);
  function start(){observer.observe(document.documentElement,{subtree:true,childList:true,attributes:true,attributeFilter:['data-nexlab-team-id','data-nexlab-record-id']});document.addEventListener('pointerdown',event=>closeTaskFloatingPanels(event.target),{passive:true});document.addEventListener('keydown',event=>{if(event.key==='Escape')closeTaskFloatingPanels(event.target,{escape:true});});void resolveNotificationTarget().then(()=>scheduleScan());scheduleScan();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();

  globalThis.addEventListener('offline',()=>{for(const state of stateByTeamId.values())setCommunicationState(state,'offline','O dispositivo está offline.');});
  globalThis.addEventListener('online',()=>{for(const state of stateByTeamId.values()){if(state.activeTab!=='conversation')continue;setCommunicationState(state,'reconnecting');if(!state.loading)void loadMessages(state,{force:true});}});
  document.addEventListener('visibilitychange',()=>{if(document.visibilityState!=='visible')return;for(const state of stateByTeamId.values()){if(state.activeTab!=='conversation'||state.loading||!state.loaded)continue;const last=Number(state.communication?.lastSuccessAt||0);if(!last||Date.now()-last>120000)void loadMessages(state,{force:true});}});
  globalThis.addEventListener('nexlab:session-reset',()=>{profilesPromise=null;notificationPromise=null;pendingNotification=null;for(const state of stateByTeamId.values()){if(state.taskRealtimeTimer)clearTimeout(state.taskRealtimeTimer);try{state.taskRealtimeUnsubscribe?.();}catch{}state.taskRealtimeTimer=null;state.taskRealtimeUnsubscribe=null;state.composerApi=null;}stateByTeamId.clear();try{sessionStorage.removeItem('nexlabTeamTaskTarget');}catch{}});
  globalThis.__NEXLAB_TEAM_CHAT__=Object.freeze({
    version:BUILD.version,revision:REVISION,workerUrl:workerUrl(),
    refreshCurrent(){const panel=document.querySelector(PANEL_SELECTOR);const state=panel&&states.get(panel);return state?loadMessages(state,{force:true}):Promise.resolve(null);},
    refreshTasks(){const panel=document.querySelector(PANEL_SELECTOR);const state=panel&&states.get(panel);return state?loadTeamTasks(state,{force:true}):Promise.resolve(null);},
    openCurrent(){const panel=document.querySelector(PANEL_SELECTOR);const state=panel&&states.get(panel);if(state){activateTab(state,'conversation');return true;}return false;},
    openTasks(){const panel=document.querySelector(PANEL_SELECTOR);const state=panel&&states.get(panel);if(state){activateTab(state,'tasks');return true;}return false;},
    snapshot(){const panel=document.querySelector(PANEL_SELECTOR);const state=panel&&states.get(panel);return state?Object.freeze({teamId:state.teamId,activeTab:state.activeTab,loaded:state.loaded,messages:state.messages.length,nextCursor:!!state.nextCursor,canView:state.access?.can_view_chat??null,canSend:state.access?.can_send_message??null,communicationState:state.communication?.status||null,lastSyncAt:state.communication?.lastSuccessAt||null,lastLatencyMs:state.communication?.lastLatencyMs??null,tasksLoaded:state.tasksLoaded,tasks:Array.isArray(state.taskWorkspace?.tasks)?state.taskWorkspace.tasks.length:0,canCreateTask:state.taskWorkspace?.permissions?.can_create??null,canManageTask:state.taskWorkspace?.permissions?.can_manage??null}):null;}
  });
})();
